var cnt = 0;
while(true){
	// Port 1, running of tut.script for remote machines.
	// Port 2, targeting 'best' server.
	var pr1 = readPort(1);
	if (pr1 != "NULL PORT DATA"){
		while(pr1 != "NULL PORT DATA"){
			var res = exec("tut_staging.script", "home", 1, pr1);
			print("Process count: ", ++cnt);
			print("++++++++++")

			pr1 = readPort(1);
		}
	}
}